const Loading = () => {
  return (
    <div className="flex flex-grow justify-center items-center">
      <button type="button" className="text-2xl font-medium text-green-500" disabled>
        Loading...
      </button>
    </div>
  );
};

export default Loading;
